# SQuRo_Backup 修改清单（2026-09-17）

## 状态总览

| 问题 | 状态 | 结论 |
| --- | --- | --- |
| **问题 2**：站立抖动严重、没有完全站稳 | **已完成并验收**（含人工录像复核） | 确定性站立比 600 基准静止 2.7 倍、力矩饱和 27.3% → 6.0%，成功判据可达且成功率 ~100% |
| **问题 1**：没有按照预计算表的时间走 | **本批已实现代码改动，待续训验证** | 真因不是"提速"而是**参考瞬移**（相位提前推进导致）；修复 = 阶段推进加"本段参考播完"门控，不动奖励。回归 22 项通过，手调回放 λ=1/3 仍可达 |

## 问题 1：段末门控（本批）

| 状态 | 内容 |
| --- | --- |
| 已实现 | `advance1 = p1 & s1_confirmed & (t_phase ≥ 0.80λ)`；`advance2 = p2 & s2_confirmed & (t_phase ≥ 0.15λ)` |
| 已实现 | 手调回放同步同一门控，继续充当"目标可达"闸门 |
| 已实现 | 日志位互换：`Gate/s1_advance`、`Gate/s2_advance` → `Progress/s1_lag`、`Progress/s2_lag` |
| 未改 | 奖励函数与权重、观测维度、回合长度、λ 课程、参考表；`p1_buffer_s` 保持实际秒 1.0 s（按 λ 缩放会让 λ=1 时手调参考自己被判超时） |
| 已验证 | 回归 22 项；新增"参考跨阶段连续"守卫（去掉门控即报 1.570 rad）；手调回放 λ=1/λ=3 均 `phase=DONE` 零重试 |
| 待验证 | 续训后 `s1_lag`/`s2_lag` 是否收敛到 ±0.15λ。**成功率会先跌后重建，不能沿用旧基线** |

## 问题 2：本批实现与验收

当前基线为 `2026-09-17_17-14-06/model_800.pt`（从 `2026-09-17_15-00-33/model_600.pt` 恢复，`entropy_coef` 降到 0.003）。

两个严重问题按顺序逐项处理：本批只做问题 2，验收通过后再单独开批做问题 1。两件事都涉及结算层，但不**同时**改。

### 问题定位（先量化，不改代码）

同一批**权重无关**量的确定性回放显示：降熵把 σ 从 3.030 压到 0.759 并保持平稳，
但站立抖动只改善了约 1/3（脊柱速度 RMS 6.88 → 5.22），离"站稳"很远
⇒ **抖动的主因不是探索噪声**。

根因是成功判据不要求"站住"而里程碑奖励"早"（`60·γ^T`），"快而猛"严格优于"慢而稳"。
完整数据（600/800/900 对照表、赶时间收益实测）与实测口径见
`docs/SQuRo_Backup_奖励对照.md` 的"站立成功加入稳定性判据"一节，此处不重复。

### 本批改动（一个批次，判据结构与静止奖励一起改）

| 状态 | 内容 | 规则或验收 |
| --- | --- | --- |
| 已实现 | 成功判据加稳定性条件 | 站立窗口内**平均**关节速度 ≤ `STAND_VEL_MEAN_MAX = 3.5` rad/s（14 驱动关节瞬时 RMS 的窗口均值）；站立参考速度恒为 0 |
| 已实现 | 确认时长 0.5 → **1.5 s** | `timing.STAND_CONFIRM_DURATION = 1.5`，训练与手调两侧共同引用（`verify_backup_config` 第 3 节校验一致） |
| 已实现 | 判据用窗口均值，不用连续达标时长 | `BackupCommand._update_stand_window`：窗口成立累加 `T` 与 `V = ∫vel_rms dt`，中断则一起清零；结算 `T ≥ 1.5 s 且 V/T ≤ 3.5` 且当步满足严格几何 |
| 已实现 | 新增 `stand_still` 静止奖励 | P3 且严格几何成立时 `(1 − vel_rms/6).clamp(0,1)`，`weight_stand_still = 3.0`，与判据共用同一个 `vel_rms` |
| 已实现 | 新增标定日志 | `Progress/stand_hold`（窗口时长）与 `Progress/stand_mean_vel`（**判据量本身**），均不参与判据 |
| 已实现 | 回放与训练同源 | 手调脚本 P3 分支原先自己数"几何连续步数"，比训练宽松、会在训练判失败时仍打印"稳定站起"；已改为直接读训练环境的 `stand` 终止项 |
| 已验证 | CPU 回归 21 项通过 | 新增窗口均值判据、清零重启（非暂停）、静止奖励 P3 门控与线性核三项 |
| 已验证 | 手调版本仍可达 | `SQuRo_Backup_Replay.py --visualize none` 在 λ=1/λ=3 均走到 `[RESULT] phase=DONE`，走的是"训练环境确认稳定站起"分支 |
| **已验收** | **抖动已修好（问题 2 关闭）** | run `2026-09-17_19-54-10_S2_stand_window`（800→1099，300 轮）：`timeout` 归零、成功率 ~100%、回合 276.9 步；确定性回放同口径对照 600：脊柱 2.12→**0.61**、腿 3.35→**1.26**、14 关节合成 2.78→**1.03**、力矩饱和 27.3%→**6.0%**、目标角抖动 0.061→**0.028**、未加权站立窗口步奖励 0.2884→**0.3338** |
| 遗留脆弱性 | 成功余量只有 2% | `Progress/stand_mean_vel` 3.43 vs 门限 3.5，且 `Policy/mean_std` 300 轮内 0.296→0.329 单调上升。它是**采样量**（= 确定性质量 ⊕ σ 噪声），确定性质量已经很好，这 3.43 几乎全是噪声。σ 只被离散事件塑造（见技术细节 §7.2.4），所以加压只能靠**判据阈值课程**，加大 `stand_still` 权重无效 |

### 已撤回：判据的第一版实现（"连续达标 + 2 倍侵蚀"）

第一版用 `STAND_VEL_RMS_ENTER/STAY` 双阈值 + 短暂掉出按 `STAND_CONFIRM_DECAY = 2.0` 侵蚀，
实测 219 轮 `stand` 终止项全程为 0。复盘结论是**判据结构隐含了占空比门槛** `d > 2/3`，
而策略实测 `d = 0.55` ⇒ 结构上不可达。推导与数据见
`docs/SQuRo_Backup_技术细节.md` §7.2.1，此处不重复。

**教训（保留）**：写"连续达标 N 秒"类判据前，先算清它对占空比的要求；
只要存在"净漂移 < 0 的占空比区间"，判据就可能在该区间内永远不可达 —— 这是阈值怎么调都救不回来的结构问题。

### 本批边界

- **不动**共享的 `BackupCommand._update_confirmation`：那套是 S1/S2/倒置判据用的硬清零语义，
  改成衰减会影响阶段推进与重试规则。站立另走一套。
- **不全局加大** `action_L2`/`action_L1` 权重、**不恢复** `clip_actions`；静止奖励是**P3 专属**的，
  且用"奖励"而不是"惩罚"（惩罚会给出"不进锥就不被罚"的作弊路线）。
- **不动**参考时钟与结算层（问题 1）：不引入 `t_cycle`、不改里程碑发放时刻、不动 `stand` termination 的 `time_out=True`。
- **不动** γ、回合长度、rollout 长度、网络结构与 PPO 其他参数。
- **不动**参考表、状态机阶段划分、S1/S2 判据与其余奖励权重。
- 阈值改动的风险是"成功变得不可达 ⇒ 里程碑归零 ⇒ 策略退化"。
  这是可回滚的单点常量（`mdp/timing.py`），先看 `Progress/stand_mean_vel`（判据量本身）
  与 `Policy/mean_std`，再决定收紧还是放松；`Progress/stand_hold` 回答"窗口时长够不够"。
- 用手调回放（λ=1/λ=3）作为"目标可达"的可行性闸门；回放必须与训练**同源**才有意义。
- 新增 `stand_still` 奖励**只作用于 P3**，且用奖励而不是惩罚；它同时是"判据可达性"的加速器
  （给同方向的稠密梯度），不是判据的替代品。

---

# 历史草案（归档，不作为当前实施指令）

以下为**上一批**（S2 抬身路径 + P2 确认宽限）的执行版记录，以及更早的 A/B/C/D/E 批草案。
A2–A5 的联改设计（`t_nom` 结算、固定循环、去掉 `stand` termination、中途部分复位）已被撤回：
用 `t_nom` 当结算时钟是坏设计——阶段推进由姿态驱动，达标的瞬间 `t_nom` 就会跳到 0.80，
等于立刻结算；A2/A4 在"迟到结算"与"循环复位"上自相矛盾；A3 只保证最后 0.5 s；
改共享 `_update_confirmation` 是错的；A4 低估了复位的边界工作量；A5 需要成功事件全量迁移。

**若与上方当前执行版冲突，以上方为准；不得直接按以下"实施顺序"修改代码。**

### 上一批（S2 抬身路径 + P2 确认宽限）记录

| 状态 | 内容 | 规则或验收 |
| --- | --- | --- |
| 已实现 | S2 允许两条路径 | 原贴地路径：两段朝向在 45° 内、各自高度 <0.04 m；新增抬身路径：两段各自背腹余弦 >0.9、各自高度 >0.05 m |
| 已实现 | 连续确认与顺序不变 | 两条路径的联合候选必须连续有效 0.10 实际秒；只在 P2 推进到 P3，不能跳过 S1；每回合 S2 奖励一次 |
| 已实现 | P2 确认宽限 | 原截止 `0.15λ+0.30 s`；仅候选仍有效且未确认时暂缓重试，最晚到原截止后 0.10 实际秒；候选中断立即按原超时规则重试，确认成功优先 |
| 已实现 | 参考与手调边界同步 | 宽限期间保持 T3 末端，不将扭转参考重播到 ±1.57；手调复用 S2 判据并同步宽限，原手调回退机制不改 |
| 已验证 | CPU 回归 | 18 项通过，包括双路径、NaN/Inf、单段抬起、严格阈值、确认中断、宽限上限、多 λ、P1 不变、手调一致性与成功计时 |
| 已保留 | `entropy_coef=0.003` | L1/L2 权重仍为 0.1，不在本批改动 |
| 后续独立项 | 价值诊断 | 终止/非终止样本分别记录价值预测、目标、自举补偿；不把高 value loss 直接认定为某个已证实根因 |

本清单只覆盖**训练记账层与参考时钟层**，不含动作质量优化（C 批）与仿真精度（不在本轮范围）。
凡标【待决策】的项需要你先拍板，未决策前不实施。

---

## 0. 背景与已确认的共识

### 当前实测状态（run `2026-09-17_15-00-33`，读到 iter 1170）

| 指标 | 值 | 说明 |
| --- | --- | --- |
| 成功率 | ~100% | `Episode_Termination/timeout` 归零，全部由 `stand` 终止，`milestone_success` 饱和 |
| 回合长度 | 1000 → **142** 步 | 成功即终止 |
| `Policy/mean_std` | 1.0 → **3.909** | 持续上涨 |
| `Loss/value` | 0.065 → **261.6** | 自 iter 637（成功开始）起爆涨 |
| `Episode_Reward/action_L2` | −0.139 → **−7.199** | 与 σ² 同步（σ² ×6.95，L2 ×6.94） |
| `Train/mean_reward` | +71.6 → **−9.512** | 回合回报转负 |

### 600 vs 900 确定性回放（站立窗口，`stand_confirm_elapsed > 0.1 s`）

| 指标 | 600 | 900 | 倍数 |
| --- | ---: | ---: | ---: |
| 脊柱关节速度 RMS | 1.51 | 6.88 | 4.6× |
| 腿部力矩饱和比例 | 20.8% | 74.7% | 3.6× |
| 限幅后目标角相邻步变化 RMS | 0.040 rad | 0.482 rad | 12× |
| 站立窗口步奖励 | +0.2764 | +0.1159 | 2.4× |

### 已确认的共识

1. **从 `model_600` 出发**，不默认最后一轮最好。
2. **不全局加大平滑惩罚**（`action_L2` 已是全部密集项的 2.3 倍，其中约 4/5 是探索噪声税）。
3. **先把训练记账修对，再定义持续稳定成功，最后才做动作质量。**
4. 不恢复 `clip_actions`；不新增 P4 状态机阶段；不同时改全部变量。

### 因果链（当前认识，非已证实结论）

```
成功终止被标记 time_out
  → 该步回报目标 = r_t + γV(s_t)（自引用，rsl_rl/ppo.py:150-155）
  → Loss/value 爆涨、优势失真、熵项占主导
  → σ 1.0 → 3.9
  → action_L2 噪声税 ∝σ² 放大 7 倍
  → 密集奖励转负 → 回合回报转负 → "尽快结束回合"变成最优
  → 越跑越快越猛、站立段抖动 12×
```

同时存在三条独立的"跑快更赚"来源（λ=3 下参考需 4.4 s，实测 1.1 s 就站上）：

| 成因 | 机制 | 折扣后对"跑快"的偏向 |
| --- | --- | ---: |
| ① 折扣视野过短 | `γ=0.99^0.5` ⇒ 每秒 0.605，视野仅 2 s < 参考 4.4 s | 独立存在 |
| ② 参考被姿态牵引 | 门控按姿态推进 ⇒ 跑快参考就追上 ⇒ 跟踪误差不增长 | 跑快在密集项上零代价 |
| ③ 里程碑按达成时刻发放 | 收入 = `60·γ^T` | **+27.9** |
| 密集项（若修 ②） | 跑快 ⇒ 参考仍在 T1 ⇒ 跟踪奖励崩 | **−34.5** |
| **净** | 只修 ②③ 时参考节奏仅赢 | **+6.6（边际过小）** |

---

## 1. 需要你先拍板的决策

### 【待决策 D1】论文是否要求"按参考节奏翻正"？

- **要求** ⇒ 实施 A1（参考改时间驱动），节奏由密集项强制。
- **不要求**，只要求"能翻正站起" ⇒ **跳过 A1**，只做 A2（取消提前红利），接受"快一点也可以"。

> A1 会推翻 `c092f39` 定下的"姿态触发推进 + 超时重播"哲学（让机器人准备好就进下一阶段），改为"必须按参考节奏走"。这是任务定义层面的选择。

### 【待决策 D2】成功奖励与中途复位的触发点

| 选项 | 内容 | 后果 |
| --- | --- | --- |
| **选项 1（推荐）** | 复位挂在**循环末端**（参考时钟走完） | "尽快复位"的激励从根本上不存在；循环长度固定 |
| 选项 2 | 复位挂在"站上那一瞬"（原始提法） | 会把"尽快站上"变成"尽快复位"，**比现状更强** |

### 【待决策 D3】折扣 γ 是否调整

现状 `γ = 0.99^0.5 = 0.99499`/步 ⇒ **每秒 0.605，有效视野 2 s**，而 λ=3 的参考需要 4.4 s。
**视野短于任务时长**，agent 天然短视。候选：`0.999^0.5`（视野 ~4 s）或 `0.999`。
属独立对照项，会影响 GAE 偏差/方差与 PPO 稳定性，**不建议与 A 批同时改**。

### 【待决策 D4】回合长度与 rollout 长度是否调整

- 一个循环 = `2.5λ` = 5~10 s（λ∈[2,4]），当前回合 10 s ⇒ 一回合只装 1~2 个循环。
- `_STEPS_PER_ITER = 96`（0.96 s）**远短于一个循环**，GAE 几乎全靠 bootstrap。
- 候选：`episode_length_s` 10 → 20 s；`_STEPS_PER_ITER` 96 → 200~400。
- 同样属独立对照项。

---

## 2. 修改项清单

### A 批：参考时钟与结算层（核心，A2~A5 必须一起改）

#### A1 【待 D1】参考改为时间驱动

- **目的**：让"跑快"在密集项上有代价，恢复参考的时间语义。
- **内容**：
  - `mdp/command.py`：新增每环境 `t_cycle`（实际秒）与每循环重采样的 `lam`；`t_nom = clamp(t_cycle / lam, 0, REFERENCE_TOTAL_TIME)`。
  - `phase` 改由 `t_nom` 分区：`< P1_END → 0`、`< P2_END → 1`、`else 2`。
  - `mdp/reference.py`：`_stage_t_nom` 退化为 `clamp(t_cycle/λ)`；删除 p1_t/p2_t/p3_t 分段与 `p2_endpoint` 专用表（P2 的"保持"由时间自然产生）。
  - `_update_command` 中删除 `advance1/advance2` 对 `phase` 的写入。
  - **保留** `p1 = (phase == 0)` / `p2 = (phase == 1)` 布尔，供里程碑窗口判定使用。
  - `retry1/retry2` 随 A1 一并停用（循环长度固定，超时重播不再有意义）；这同时使 **D1（重试参考平移）自动消失**。
- **验收**：确定性回放里"实际到达 S1 的时间"应接近 `0.8λ`（λ=3 时约 2.4 s），而不是 0.26 s。
- **风险**：高。若策略暂时达不到姿态，密集奖励会长期偏低，需要 λ 课程与足够训练量兜底。**这也是 D1 必须先决策的原因。**

#### A2 里程碑改为"锁存 + 定时结算"

- **目的**：消除"提前完成"的折扣红利（即你提出的离散奖励数值问题）。
- **内容**：
  ```
  achieved_s1 = 锁存(S1 姿态连续保持 pose_confirm_s)      # 每循环一次
  settled_s1  = 一次性
  pulse_s1    = achieved_s1 & (t_nom >= P1_END) & ~settled_s1
  ```
  S2 同理，窗口为 `t_nom >= P2_END`。
  - `_s1_awarded` / `_s2_awarded` 语义从"已发放"拆成"已达成"与"已结算"两个标志。
  - 奖励函数（`compute_s1_milestone_reward` 等）读取的脉冲接口不变，**权重不变**。
  - 若姿态达成晚于参考末端，则在达成那一刻结算（只取消"快"的红利，不惩罚"慢"）。
- **验收**：确定性回放里 S1/S2 的入账时刻应分别落在 `0.8λ` / `0.95λ` 附近，而不是 0.26/0.48 s。
- **风险**：低。不改变量级，只改变发放时刻。

#### A3 成功改为"循环末端保持站立"

- **目的**：让"站住"成为**被要求**的条件；消除"站上即结算"。
- **内容**：
  ```
  success_pulse = (t_nom >= REFERENCE_TOTAL_TIME)
                  & (站立连续计时 >= STAND_CONFIRM_DURATION)
                  & ~settled_success
  ```
  - 站立判据沿用 `command.standing_state()`（两段背腹余弦 > 0.9、各自高度 > 0.05），不新增判据。
  - **配套必须做**：`_update_confirmation` 增加"允许短暂掉出"（衰减而不是清零）。现在任何一帧候选为假就清零，长窗口下会被单帧接触瞬态反复打断。
  - "从站上到参考末端保持站立"自动成为要求，**不需要单独设计保持时长参数**（即原 B1 的目的由本项承担）。
- **验收**：确定性回放下成功入账时刻接近 `REFERENCE_TOTAL_TIME × λ`。
- **风险**：中。若策略无法保持，成功率下降；需先在 λ∈[2,4] 下确认 `model_600` 能保持。

#### A4 中途复位（部分复位）

- **目的**：一回合内多次尝试；避免 `time_out` 自举；避免回合长度博弈。
- **内容**：
  - 新增 `command.resample_cycle(env_ids)`：`t_cycle = 0`、重采样 λ、清确认计时 / `_stand_elapsed` / 锁存与结算标志 / `_last_*`。
  - `mdp/events.py`：把"写仰卧初态"抽成 `apply_fallen_state(env, env_ids)`，`reset_model` 与中途复位共用同一份代码。
  - 触发时机：`t_nom >= REFERENCE_TOTAL_TIME`（循环走完）时，**先结算成功脉冲，再对满足条件的 env 执行复位**。
  - **严禁调用 `env.reset(env_ids)`**：它会重置 `episode_length_buf` 并让 `RewardManager` 结算并清零 episode 累计，等于结束回合。
  - **不动**：`episode_length_buf`、奖励缓冲与 episode 累计、`common_step_counter`、RL 侧状态与观测归一化。
  - 需要一个能对 **env 子集**生效的路径：优先在 `command._update_command` 内直接调用（它已能拿到 env 掩码），避免改动核心 `env.step`。
- **验收**：`Train/mean_episode_length` 回到固定值；确定性回放里一回合内出现多个循环。
- **风险**：中。物理上是状态跳变，需确认不产生异常接触冲量（现有 reset 用 `z=0.024` 贴地，已在实际训练中验证可行）。

#### A5 去掉 `stand` termination

- **目的**：消除 `time_out=True/False` 的两难（自引用自举 vs 提前结束弃值）。
- **内容**：`SQuRo_Backup_env_cfg.py` 的 `terminations` 只保留 `timeout`；`check_stand_success` 不再作为 termination，成功改由 A3 的脉冲承担。
- **影响**：`Episode_Termination/stand` 消失；`Episode_Termination/timeout` 恢复为唯一的终止来源。
- **验收**：日志中不再出现 `Episode_Termination/stand`；出现新的成功结算脉冲。

### B 批：探索成本对照（**不预设结论**）

- **目的**：判断 σ 上升与探索成本、价值目标的关系，决定 L2 与熵系数要不要改。
- **内容**：从同一 `model_600` 分出三条短程对照（各约 200 轮）：

  | 组 | 配置 |
  | --- | --- |
  | 对照 | 原配置续训（`entropy_coef=0.01`、`weight_smooth_L2=0.1`） |
  | 组 1 | 只降 `entropy_coef` 0.01 → 0.003 |
  | 组 2 | 降 `entropy_coef`，且 `weight_smooth_L2` 0.1 → 0.01 |

- **明确**：按你的判断，**不预设 σ 必然下降**（熵项在 loss 中、L2 在奖励中，两者不能直接相减求平衡点；且熵 ×0.3 / L2 ×0.1 的推力比反而变成 3 倍）。是否必须降 L2 由对照决定。
- **验收（权重无关）**：见 §4。

### C 批：动作质量（第二阶段，仅登记，暂不实施）

| 项 | 内容 | 备注 |
| --- | --- | --- |
| C1 | 速度约束形式改造：用 Huber/线性替代已饱和的 `exp(-0.5·mse)` 指数核 | 指数核在高误差区奖励变化极小，给不出足够回报差异；但线性成本会增加持续成本，**不因为"有梯度"就认定更有效** |
| C2 | `last_action` 观测加 `clip` | **独立消融项，不是零风险清理**：会压缩不同大动作、改变已有策略与归一化器面对的分布；动作历史本身可能有助于抑制振荡 |
| C3 | 只在 P3 做针对性动作质量约束 | 必须在 A3 落地后（否则会激励"更快冲过 P3"） |

### D 批：遗留陷阱（**都会改变训练行为，需独立排期**）

| 项 | 现状 | 备注 |
| --- | --- | --- |
| D1 | 相位重试把参考平移 1.570 rad | **A1 落地后 retry 停用 ⇒ 本项自动消失**，届时复查 |
| D2 | `backup_body_traj.npy` 是 λ=1 录制、用在 λ∈[2,4]；P2_END 处完美跟踪者 r_height 仅 0.731 | 属行为改动（改高度奖励信号），独立排期 |
| D3 | ~~`compute_stand_still_penalty` 仍用基座重力投影~~ | **已处理**：该函数已重写为 `compute_stand_still_reward`，门控统一走 `stand_gate()` 的分段 site 判据，并已注册（本批） |
| D4 | 指标命名：`Progress/relapse` 含初始仰卧；`Progress/enter_p3` 不是成功率 | 纯命名/口径，可与 E 批一起 |

### E 批：日志与验收口径

| 项 | 内容 |
| --- | --- |
| E1 | 成功率口径改为 **成功结算次数 / 循环数**；不再用 `Progress/success` 的逐步均值 |
| E2 | **保留两套指标**：新配置奖励（检查新目标是否正常）+ 固定旧权重重算的奖励与权重无关量（判断策略是否真正改善） |
| E3 | `Policy/mean_std`、`Loss/value` 只作趋势观察，**不设硬阈值**（`use_clipped_value_loss=True` 下 `sqrt(Loss/value)` 不是普通预测 RMSE） |
| E4 | 增补价值诊断：价值预测与目标的尺度、解释方差、**终止步 / 非终止步分别误差** |

---

## 3. 实施顺序

| 步 | 内容 | 前置 |
| --- | --- | --- |
| 1 | 确认 D1~D4 | — |
| 2 | 实施 **A2 + A3 + A4 + A5**（若 D1=要求，加 A1） | 步骤 1 |
| 3 | 从 `model_600` 续训短程（约 100~200 轮），按 §4 验收 | 步骤 2 |
| 4 | 跑 B 批三条对照 | 步骤 3 |
| 5 | 视结果进入 C 批 | 步骤 4 |
| 6 | D2/D3/D4 独立排期 | — |

**关于续训的说明**：默认 resume 会一并恢复 critic、优化器与课程计数（`runner.py` 里 `common_step_counter` 也从 `infos["env_state"]` 恢复），**不只是恢复"好策略 + 好 σ"**。需明确是否接受，或只加载 actor。

---

## 4. 验收指标（全部为权重无关量）

| 类别 | 指标 | 期望方向 |
| --- | --- | --- |
| 记账健康 | `Loss/value` 是否随成功次数上升而**保持平稳**（不再单调上涨） | 平稳 |
| 记账健康 | `Policy/mean_std` 是否停止上涨 | 停止上涨 |
| 时序 | 确定性回放中实际到达 S1 / S2 / 成功的时间，对比 `0.8λ` / `0.95λ` / `2.5λ` | 接近参考 |
| 动作质量 | 站立窗口内未加权 `\|Δa\|` RMS（**不是** `Episode_Reward/action_L2`） | 向 600 的 0.13 靠 |
| 动作质量 | 站立窗口内目标角相邻步变化 RMS | 向 600 的 0.040 rad 靠 |
| 动作质量 | 站立窗口内关节速度 RMS（脊柱/腿） | 向 600 的 1.51 / 2.62 靠 |
| 动作质量 | 腿部力矩饱和比例 | 向 600 的 20.8% 靠 |
| 成功定义 | 成功结算次数 / 循环数；回合长度 | 不再要求回合变长 |

**已废弃的验收方式（曾用过，属循环论证）**：用 `Episode_Reward/action_L2` 从 −50 降到 −5 证明策略变好——权重降 10 倍它就自动降 10 倍。

---

## 5. 明确不做

| 不做 | 原因 |
| --- | --- |
| 全局加大平滑惩罚 | `action_L2` 已是全部密集项的 2.3 倍、约 4/5 是噪声税 |
| 恢复 `clip_actions` | 裁剪是信息屏障；去掉后腿指令从 −22 回到 −7、超出量降 12.8 倍 |
| 新增 P4 状态机阶段 | 由 A3 的"循环末端保持"承担同样语义，不动状态机 |
| 把抖动归因于仿真精度 | 确定性策略已有目标角振荡证据（站立段每步 0.48 rad、力矩饱和 75%） |
| 未经对照把 `time_out` 改回 `False` | 会重新引入"提前结束弃值"的争议 |
| 一次性改全部变量 | 历史上"权重 + 裁剪"同改导致无法归因 |

---

## 6. 改动文件预估

| 文件 | 涉及项 |
| --- | --- |
| `mdp/command.py` | A1 A2 A3 A4（时钟、分区、锁存结算、`resample_cycle`、确认计时抗抖动） |
| `mdp/reference.py` | A1（`_stage_t_nom` 退化、删 `p2_endpoint`） |
| `mdp/terminations.py` | A5（移除 stand termination） |
| `mdp/events.py` | A4（抽出 `apply_fallen_state`） |
| `SQuRo_Backup_env_cfg.py` | A5（term 只留 timeout、成功脉冲绑定） |
| `mdp/curriculums.py` | B 批（仅在对照结论出来后） |
| `config/rl_cfg.py` | B 批、D3 |
| `scripts/Backup/verify_backup_stage_rewards.py` | 全部（回归用例需按新时钟语义改写） |
| `scripts/Backup/*` | 诊断脚本需支持"按循环"统计 |
| `docs/SQuRo_Backup_技术细节.md` / `奖励对照.md` | 记录新时钟语义与实测 |
